import { StyleSheet} from 'react-native';

export const headerStyle = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: 'blue',
      alignItems: 'flex-end',
      
    },
  });